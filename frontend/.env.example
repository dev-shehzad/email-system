# Frontend Environment Variables

# API Base URL
# Development: http://localhost:8000
# For Vercel rewrite (no DNS): use /api so frontend calls are proxied by Vercel
# Production (with Vercel rewrite):
VITE_API_BASE_URL=/api
